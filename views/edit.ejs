<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Edit Post</h1>

    <% if (errorMessage) { %>
        <div class="alert alert-danger"><%= errorMessage %></div>
    <% } %>

    <form action="/editPost/<%= post.blog_id %>" method="POST">
        <input type="text" name="postTitle" value="<%= post.title %>" required>
        <input type="text" name="postAuthor" value="<%= post.creator_name %>" required>
        <textarea name="postContent" required><%= post.body %></textarea>
        
        <% if (post.creator_user_id === currentUserId) { %> 
            <button type="submit">Update Post</button>
        <% } else { %>
            <p>You do not have permission to edit this post.</p>
        <% } %>
    </form>
    <a href="/home">Back to Home</a>
</body>
</html>
